# Dotfiles

```
git clone --recurse-submodules -j$(nproc) https://github.com/junglie85/dotfiles-new.git ~/.dotfiles
ln -s ~/.dotfiles/zsh/.zshrc .zshrc
ln -s ~/.dotfiles/git/.gitconfig .gitconfig
```
